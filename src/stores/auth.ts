import {create} from 'zustand';import {supabase} from '../lib/supabase'
type AuthUser={email?:string;user_metadata?:{name?:string}}
type Auth={user:AuthUser|null;ready:boolean;init:()=>Promise<void>;signIn:(e:string,p:string)=>Promise<void>;signUp:(n:string,e:string,p:string)=>Promise<void>;signOut:()=>Promise<void>}
export const useAuth=create<Auth>(set=>({user:null,ready:false,init:async()=>{if(!supabase){set({ready:true});return}const{data}=await supabase.auth.getUser();set({user:data.user,ready:true});supabase.auth.onAuthStateChange((_e,s)=>set({user:s?.user??null}))},signIn:async(email,password)=>{if(!supabase)throw new Error('Supabase 환경변수가 설정되지 않았습니다.');const{error}=await supabase.auth.signInWithPassword({email,password});if(error)throw error},signUp:async(name,email,password)=>{if(!supabase)throw new Error('Supabase 환경변수가 설정되지 않았습니다.');const{error}=await supabase.auth.signUp({email,password,options:{data:{name:name.trim()},emailRedirectTo:`${location.origin}/login?verified=1`}});if(error)throw error},signOut:async()=>{if(supabase)await supabase.auth.signOut();set({user:null})}}))
