import{useEffect,useState}from'react';import{createPortal}from'react-dom';import{useParams}from'react-router-dom';import{useGifts}from'../stores/gifts';import type{BirthdayGift,GiftStep}from'../types'
type P={gift:BirthdayGift;next:(s:GiftStep)=>void}
export default function GiftPage(){const{publicId}=useParams(),{fetchPublic,loading}=useGifts(),[gift,setGift]=useState<BirthdayGift>(),[error,setError]=useState(''),[step,setStep]=useState<GiftStep>('landing'),[progress,setProgress]=useState(0),[rejects,setRejects]=useState(0);useEffect(()=>{fetchPublic(publicId!).then(g=>g?setGift(g):setError('이 Birthday Box는 존재하지 않거나 비공개예요.')).catch(()=>setError('선물을 불러오지 못했어요.'))},[publicId,fetchPublic]);function next(s:GiftStep){setStep(s);scrollTo({top:0,behavior:'smooth'});if(s==='loading')[0,37,68,99,100].forEach((n,i)=>setTimeout(()=>{setProgress(n);if(n===100)setTimeout(()=>setStep('gift-reveal'),600)},[100,700,1400,2100,3300][i]))}if(loading)return <main className="stage"><div className="loader"/></main>;if(error)return <main className="stage"><div className="big-icon">📭</div><h1>선물을 찾지 못했어요</h1><p>{error}</p></main>;if(!gift)return null;const common={gift,next} as const;return <main className="gift-page">{step==='landing'&&<Landing {...common}/>} {step==='recipient-check'&&<Check {...common}/>} {step==='loading'&&<Loading gift={gift} progress={progress}/>} {step==='gift-reveal'&&<Reveal {...common} rejects={rejects} reject={()=>setRejects(n=>n+1)}/>} {step==='contract-complete'&&<Complete {...common}/>} {step==='contract'&&<Contract {...common}/>} {step==='birthday'&&<Finale gift={gift}/>}</main>}
function Landing({gift,next}:P){return <section className="stage landing"><div className="orb one"/><div className="orb two"/><p className="service-badge">BIRTHDAY BOX</p><div className="gift-hero">🎁</div><p className="eyebrow">A SPECIAL GIFT FOR YOU</p><h1><em>{gift.recipientName}</em>님에게<br/>특별한 생일 선물이<br/>도착했어요!</h1><p className="muted">누군가 정성껏 준비한 선물이에요.<br/>지금 바로 확인해 보세요.</p><button className="btn primary gift-btn" onClick={()=>next('recipient-check')}>선물 확인하기 →</button><small>Birthday Box는 재미를 위한 독립 서비스입니다.</small></section>}
function Check({gift,next}:P){const[birthday,setBirthday]=useState(''),[message,setMessage]=useState('');function verify(){if(!birthday){setMessage('생일을 입력해 주세요.');return}if(birthday!==gift.birthday){setMessage('생일이 일치하지 않아요. 다시 확인해 주세요.');return}setMessage('');next('loading')}return <section className="stage"><button className="back" onClick={()=>next('landing')}>←</button><div className="big-icon bob">👋</div><p className="eyebrow">RECIPIENT CHECK</p><h1>잠깐, 본인 확인할게요</h1><div className="question-card"><span>선물 수령 대상자</span><strong>{gift.recipientName}님이 맞으신가요?</strong><label className="birthday-password">생일을 입력해 주세요<input type="date" value={birthday} onChange={e=>{setBirthday(e.target.value);setMessage('')}} max="9999-12-31" aria-describedby="birthday-error"/></label>{message&&<p id="birthday-error" className="verify-error" role="alert">{message}</p>}</div><button className="btn primary gift-btn" onClick={verify}>본인 확인하고 선물 보기</button><p className="lock">🔒 입력한 생일은 본인 확인에만 사용됩니다.</p></section>}
function Loading({gift,progress}:{gift:BirthdayGift;progress:number}){return <section className="stage"><div className="scan"><span>🎁</span><i/></div><p className="eyebrow">SEARCHING GIFT</p><h1>{gift.recipientName}님의 선물을<br/>확인하고 있어요...</h1><div className="progress-wrap"><div className="progress-label"><span>선물 조회 중</span><b>{progress}%</b></div><div className="progress"><i style={{width:`${progress}%`}}/></div><p>{progress===99?'거의 다 찾았어요! 조금만 기다려 주세요 👀':progress===100?'🎉 선물을 찾았습니다!':''}</p></div></section>}
function Reveal({gift,next,rejects,reject}:P&{rejects:number;reject:()=>void}){const words=['정말 거절할 거야? 🥺','거절하지 마...','선물 받아줘!','우리 평생 친구잖아 💗','한 번만 다시 생각해 봐','이 버튼은 잡을 수 없지 😝','수령하기가 더 쉬울걸?','친구야, 제발 받아줘 🎁'];return <><section className="stage reveal"><div className="spark">✦</div><p className="eyebrow">CONGRATULATIONS!</p><h1>축하합니다, {gift.recipientName}님!</h1><p>이번 생일 선물은 바로...</p><article className="ticket"><div className="ticket-top"><span>💖</span><small>BIRTHDAY SPECIAL TICKET</small><h2>{gift.gift.title}</h2></div><div className="price"><del>₩{gift.gift.originalPrice.toLocaleString()}</del><strong>₩{gift.gift.salePrice.toLocaleString()}</strong><span>생일 특별가</span></div><dl><div><dt>유효기간</dt><dd>{gift.gift.validity}</dd></div><div><dt>양도</dt><dd>불가능</dd></div><div><dt>환불</dt><dd>불가능</dd></div></dl><div className="barcode">|||| ||| || |||| ||| |</div></article><div className="reveal-actions"><p className="reject-message" aria-live="polite">{rejects?words[(rejects-1)%words.length]:''}</p><button className="btn primary gift-btn" onClick={()=>next('contract-complete')}>수령하기</button>{!rejects&&<RunawayButton reject={reject} count={0}/>}</div></section>{rejects>0&&createPortal(<RunawayButton reject={reject} count={rejects}/>,document.body)}</>}
function RunawayButton({reject,count}:{reject:()=>void;count:number}){const seed=count||1,random=(salt:number)=>{const x=Math.sin(seed*999+salt*77)*43758.5453;return x-Math.floor(x)},x=16+random(1)*68,y=8+random(2)*84,scale=.5+random(3)*.7,opacity=.28+random(4)*.72,rotation=-35+random(5)*70,hue=Math.floor(random(6)*35);const escape=(e:React.SyntheticEvent)=>{e.preventDefault();e.stopPropagation();reject()};return <button type="button" className={`btn ghost reject runaway ${count?'is-running':''}`} style={count?{left:`${x}vw`,top:`${y}dvh`,opacity,transform:`translate(-50%,-50%) scale(${scale}) rotate(${rotation}deg)`,backgroundColor:`hsla(${hue},100%,97%,.82)`,borderRadius:`${10+random(7)*26}px`,filter:`saturate(${.7+random(8)}) blur(${random(9)>.86?1:0}px)`}:undefined} onMouseEnter={escape} onPointerEnter={escape} onPointerDown={escape} onTouchStart={escape} onFocus={escape} onClick={escape}>거절하기</button>}
function Complete({gift,next}:P){return <section className="stage"><div className="checkmark">✓</div><p className="eyebrow">CONTRACT COMPLETE</p><h1>계약이 완료되었습니다!</h1><p className="muted">이제 공식적으로 평생 친구가 되었어요 🎉</p><div className="summary"><div><span>계약기간</span><b>{gift.gift.validity}</b></div><div><span>중도해지</span><b>불가능</b></div><div><span>환불</span><b>불가능</b></div><div><span>고객센터</span><b>{gift.gift.customerService}</b></div></div><button className="btn primary gift-btn" onClick={()=>next('contract')}>계약서 확인하기</button></section>}
function Contract({gift,next}:P){return <section className="stage contract-stage"><p className="eyebrow">OFFICIAL DOCUMENT</p><article className="paper"><div className="paper-head"><span>📃</span><h1>평생 친구 계약서</h1><p>본 계약서는 서로의 우정과 행복을 위해 작성되었습니다.</p></div><ol>{gift.contractTerms.map((t,i)=><li key={t.id}><b>제 {i+1}조</b><span>{t.content}</span></li>)}</ol><div className="sign"><small>계약자</small><strong>{gift.recipientName} <span>🤝</span> 나</strong><p>{new Date().toLocaleDateString('ko-KR')}</p></div><div className="stamp">평생<br/>친구</div></article><button className="btn primary gift-btn" onClick={()=>next('birthday')}>계약 완료</button></section>}
function Finale({gift}:{gift:BirthdayGift}){const confetti=Array.from({length:45}),captions=['참 많이 웃었던 날 ✨','함께여서 더 좋았던 순간','이날의 우리, 꽤 귀엽다 💗','잊고 싶지 않은 행복한 하루','사진만 봐도 웃음이 나와 😆','우리의 소중한 한 페이지','다음 추억도 같이 만들자 📸','평범해서 더 특별했던 날','시간이 지나도 간직할 순간','앞으로도 잘 부탁해 💗','또 하나의 레전드가 된 날 😂','함께한 모든 날이 선물이야','사진 속 웃음 그대로 행복하자','우리 우정, 영원히 저장 완료!','다시 돌아가도 똑같이 웃을 날'];return <section className="finale"><div className="confetti" aria-hidden>{confetti.map((_,i)=><i key={i} style={{left:`${(i*37)%100}%`,background:['#ff6b81','#ffcf5c','#7dd3c7','#b99cff'][i%4],animationDelay:`${(i%12)/8}s`}}/>)}</div><div className="finale-hero"><p className="eyebrow">SURPRISE!</p><div className="cake">🎂</div><h1>HAPPY BIRTHDAY<br/><em>{gift.recipientName}</em></h1><span>우리의 추억을 내려서 확인해 봐 ↓</span></div><div className="memories"><p className="eyebrow">OUR MEMORIES</p><h2>함께라서 더 빛났던 순간들</h2>{gift.photos.map((p,i)=><figure key={p.id} className={i%2?'tilt-right':'tilt-left'}><img src={p.url} loading="lazy" alt={`${gift.recipientName}님과의 추억 ${i+1}`}/><figcaption>{captions[(i*7+gift.recipientName.length)%captions.length]}</figcaption></figure>)}</div><article className="letter"><div className="envelope">💌</div><p className="eyebrow">A LETTER FOR YOU</p><h2>To. {gift.recipientName}</h2><p>{gift.letter}</p><div className="letter-end">올해도 행복한 일만 가득하길<br/><strong>🎂 💗</strong></div></article><footer>Made with love by Birthday Box</footer></section>}
