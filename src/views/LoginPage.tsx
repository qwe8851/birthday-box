import{FormEvent,useState}from'react';import{useNavigate}from'react-router-dom';import{useAuth}from'../stores/auth';export default function LoginPage(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),signIn=useAuth(s=>s.signIn),nav=useNavigate();async function submit(e:FormEvent){e.preventDefault();setError('');try{await signIn(email,password);nav('/admin/gifts')}catch(x){setError((x as Error).message)}}return <main className="auth-page"><section className="auth-card"><div className="brand-mark">🎁</div><p className="eyebrow">BIRTHDAY BOX</p><h1>다시 만나서 반가워요</h1><p className="muted">친구를 위한 특별한 장난을 준비해볼까요?</p><form onSubmit={submit}><label>이메일<input value={email} onChange={e=>setEmail(e.target.value)} type="email" autoComplete="email" required/></label><label>비밀번호<input value={password} onChange={e=>setPassword(e.target.value)} type="password" autoComplete="current-password" required/></label>{error&&<p className="error">{error}</p>}<button className="btn primary wide">로그인</button></form></section></main>}
